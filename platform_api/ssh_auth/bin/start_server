#!/bin/bash
# copy env values to use in ssh session
env | grep -e NP -e KUBERNETES >> /neuromation/.env
# stream log to stdin
touch /nonexistent/authorization.log
tail -f /nonexistent/authorization.log &
# start sshd
/usr/sbin/sshd -De
