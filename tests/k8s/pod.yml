---
apiVersion: batch/v1
kind: Job

metadata:
  name: testjob1

spec:
  template:
    spec:
      containers:
      - name: testjob1
        image: hello-world:latest
        env:
        - name: MODEL_PATH
          value: /
      restartPolicy: Never
  backoffLimit: 1
