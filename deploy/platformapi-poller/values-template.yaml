NP_CLUSTER_NAME:
NP_API_URL:
NP_ADMIN_URL:
NP_AUTH_PUBLIC_URL:
NP_JOBS_INGRESS_OAUTH_AUTHORIZE_URL:
NP_SENTRY_URL:
NP_SENTRY_CLUSTER:

image:
  repository: "$IMAGE_REPO"
  tag: "$IMAGE_TAG"

imagePullSecrets: []

replicas: 1

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0

platform:
  token: {}

jobPriorityClass:
  enabled: false

resources:
  requests:
    cpu: "1"
    memory: "1Gi"

ingress:
  enabled: false

secrets: []
