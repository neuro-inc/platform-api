
Dev : 
	- helm --set "global.env=dev" install platformapi
        - helm install traefik --name=traefik

Stage : 
	- helm --set "global.env=stage" install platformapi
        - helm install traefik --name=traefik
